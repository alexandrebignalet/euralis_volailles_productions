version: 1.0.{build}

image: Visual Studio 2017

environment:
  nodejs_version: "10.19.0"
  GH_TOKEN: 8992f483bc8e926373bb0a0093dccc7a16f9f98f

platform:
  - x64

cache:
  - node_modules
  - '%USERPROFILE%\.electron'

init:
  - git config --global core.autocrlf input

install:
  - ps: Install-Product node $env:nodejs_version
  - yarn

build_script:
  - yarn release

test: off
