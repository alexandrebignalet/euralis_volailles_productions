version: 1.0.{build}

image: Visual Studio 2017

environment:
  nodejs_version: "10.19.0"
  GH_TOKEN: bs06vMr1u4OI6a1bRDDKoYz6rcmeirN9i9afjfGZxO/sGd4+33yytYiLkhsnU9CA

platform:
  - x64

cache:
  - node_modules
  - '%USERPROFILE%\.electron'

init:
  - git config --global core.autocrlf input

install:
  - ps: Install-Product node $env:nodejs_version
  - yarn

build_script:
  - yarn release

test: off
